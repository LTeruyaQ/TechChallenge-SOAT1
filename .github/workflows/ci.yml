name: CI

on:
  push:
    branches:
      - "feature/*"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 9.0.x

      - name: Restaurar dependências
        run: dotnet restore MecanicaOS.sln

      - name: Compilar o projeto
        run: dotnet build MecanicaOS.sln --no-restore

      - name: Executar testes unitários
        run: |
          chmod +x .github/scripts/check_test_success.sh
          .github/scripts/check_test_success.sh

      - name: Construir imagem Docker para validação
        run: docker build . --file Dockerfile --tag mecanica-os-api:latest

  create-pr:
    needs: [build-and-test]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout do código completo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Instalar dependências CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y jq gh

      - name: Criar ou usar branch de release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          set -e

          echo "🔄 Atualizando informações do remoto..."
          git remote set-url origin https://github.com/${{ github.repository }}.git
          git fetch origin --prune --tags

          echo "🔍 Buscando branches de release existentes..."
          LATEST_RELEASE_VERSION=$(git ls-remote --heads origin "release/*" | awk -F'/' '{print $4}' | sort -V | tail -n 1 || true)

          if [ -z "$LATEST_RELEASE_VERSION" ]; then
            echo "⚠️ Nenhuma branch de release encontrada. Criando a primeira (release/1.0.0)..."
            LATEST_RELEASE_VERSION="1.0.0"
            git checkout main || git checkout Main
            git pull origin main || git pull origin Main
            git checkout -b "release/$LATEST_RELEASE_VERSION"
            git push origin "release/$LATEST_RELEASE_VERSION"
          else
            echo "✅ Última branch de release detectada: release/$LATEST_RELEASE_VERSION"
          fi

          RELEASE_BRANCH="release/$LATEST_RELEASE_VERSION"

          echo "🔄 Garantindo branches locais..."
          git fetch origin "$RELEASE_BRANCH":"$RELEASE_BRANCH" || true
          git fetch origin "${{ github.ref_name }}":"${{ github.ref_name }}" || true

          echo "📦 Alternando para feature atual..."
          git checkout "${{ github.ref_name }}"
          git pull origin "${{ github.ref_name }}" || true

          echo "🔍 Verificando se há diferenças entre as branches..."
          if git diff --quiet "origin/$RELEASE_BRANCH"...origin/${{ github.ref_name }}; then
            echo "⚠️ Nenhuma diferença entre a feature e a release. Nenhum PR será criado."
            exit 0
          fi

          echo "🔍 Verificando PRs abertos..."
          OPEN_PR_COUNT=$(gh pr list --state open --head "${{ github.ref_name }}" --base "$RELEASE_BRANCH" --json number | jq 'length')

          if [ "$OPEN_PR_COUNT" -eq "0" ]; then
            echo "✨ Nenhum PR aberto encontrado. Criando novo Pull Request..."
            gh pr create \
              --base "$RELEASE_BRANCH" \
              --head "${{ github.ref_name }}" \
              --title "PR automático: ${{ github.ref_name }}" \
              --body "Pull Request gerado automaticamente pela pipeline de CI. Revise as alterações."
          else
            echo "ℹ️ Já existe PR aberto para $RELEASE_BRANCH."
          fi
